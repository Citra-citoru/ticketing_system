{
  "name": "fullstack-reactjs-nestjs",
  "version": "1.0.0",
  "scripts": {
    "build:backend": "cd ticketing_api && npm install && npm run build",
    "build:frontend": "cd ticketing_web && npm install && npm run build",
    "build": "npm run build:backend && npm run build:frontend",
    "start:backend": "cd ticketing_api && npm run start:dev",
    "start:frontend": "cd ticketing_web &&  npm run start",
    "start": "npm run start:backend & npm run start:frontend",
    "test": "cypress run"
  },
  "dependencies": {
    "cypress": "^13.3.0"
  }
}
